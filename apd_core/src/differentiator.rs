use crate::api_response::PackagesData;
use crate::packages_difference::PackagesDifference;

pub fn get_packages_difference(
    packages_data_1: PackagesData,
    packages_data_2: PackagesData,
) -> PackagesDifference {
    todo!();
}
